function Light(scene, enabled, position, ambient, diffuse, specular) {
    this.scene = scene;
    this.enabled = enabled;
    this.position = position;
    this.ambient = ambient;
    this.diffuse = diffuse;
    this.specular = specular;
}